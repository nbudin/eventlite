= bootstrap_form_for [@event, cms_layout] do |f|
  = f.text_field :name
  = f.text_field :navbar_classes, label: "Navigation bar CSS classes", help: "Overrides the default CSS classes for the Bootstrap navigation bar (by default, 'navbar-light bg-faded')"
  = find_and_preserve(f.text_area(:content, :style => "font-family: monospace; min-height: 40vh;"))
  = f.submit class: 'btn btn-primary'